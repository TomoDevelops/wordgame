import { Box } from "native-base";

const Feed = () => {
    return (
        <Box flex={1} justifyContent="center" alignItems="center">
            Feed
        </Box>
    );
};
export default Feed;
